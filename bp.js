// Sum of Squares of Numbers from 1 to N

let N = 4;
let sumOfSquares = 0;

for (let i = 1; i <= N; i++) {
    sumOfSquares += i * i;
}
console.log("Sum of squares from 1 to " + N + ": " + sumOfSquares);  // Output: 30
