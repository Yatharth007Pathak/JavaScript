let str = "YathartH";
str = str.toUpperCase();  
console.log(str);

/*
Here’s a line-by-line explanation of the corrected code:

let str = "YathartH";
Declares a variable str and assigns it the string "YathartH".

str = str.toUpperCase();
Calls the toUpperCase method on the string stored in str. This method: Converts all the alphabetic characters in the string to uppercase.
Returns a new string with all characters in uppercase. The result is reassigned back to the variable str, updating its value.
Note: To "modify" a string, we must create a new one, which we did.

console.log(str);
Prints the value of str to the console. After conversion, the value of str is "YATHARTH", so this line outputs: YATHARTH
*/