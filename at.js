// You can use logical operators (&& for "and," || for "or," and ! for "not") to create complex conditions within an if statement.

let age = 25;
let hasID = true;

if (age >= 18 && hasID) {
    console.log("Access granted.");
} else {
    console.log("Access denied.");
}